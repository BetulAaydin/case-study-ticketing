server:
  port: 8082

spring:
  application:
    name: ticketing-auth
  datasource:
    url: jdbc:h2:file:./data/authdb;MODE=PostgreSQL;AUTO_SERVER=TRUE;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
  data:
    redis:
      cluster:
        nodes:
          - 10.220.15.57:6379
          - 10.220.15.58:6379
          - 10.220.38.184:6379
      password: redisClusterMayaxxx129
  h2:
    console:
      enabled: true
      path: /h2-console
      settings:
        web-allow-others: true

security:
  jwt:
    secret: ${JWT_SECRET:dGlja2V0aW5nLWNhc2Utc3R1ZHktc2VjcmV0LWtleS0yMDI2}
    algorithm: HmacSHA256
    access-ttl-minutes: 30
    refresh-ttl-days: 7

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html

management:
  endpoints:
    web:
      exposure:
        include: health,info
